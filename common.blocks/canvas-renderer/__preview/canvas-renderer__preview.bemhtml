block('canvas-renderer').elem('preview')(
    tag()('pre'),
    content()(
        function() {
            var ctx = this.ctx;
            return { elem: 'code-container', tag: 'code', content : ctx.text };
        }
    )
);
