block('canvas-renderer').elem('preview')(
    tag()('pre'),
    content()(
        function() {
            var ctx = this.ctx;
            return [
                { elem: 'code-style' },
                { elem: 'code-container', content : ctx.text }
            ];
        }
    )
);
